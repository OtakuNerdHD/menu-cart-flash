
import React from 'react';

// Componente temporariamente desabilitado para evitar conflitos com o sistema de autenticação
// O UserSwitcher foi desabilitado temporariamente durante a implementação do sistema de auth padrão do Supabase
const UserSwitcher = () => {
  // Retorna null para não renderizar nada, mantendo a compatibilidade com o App.tsx
  return null;
};

export default UserSwitcher;
